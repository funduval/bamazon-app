const mysql = require('./index.js');

var connection;

mysql.createConnection({
    host: 'localhost',
    user: 'root',
    password: 'moolet',
    database: 'mordor'
}).then(function(conn){
    connection = conn;
    conn.query('select 1+1 as res').then(function(error) {
        connection.release();
    });
});
